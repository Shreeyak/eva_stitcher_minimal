import 'package:flutter/material.dart';

// ── EVA App Color Palette ─────────────────────────────────────────────
// Material dark + deep blue accent theme.

const kBgColor = Color(0xFF070B14); // page / scaffold background
const kPanelColor = Color(0xFF0E1728); // toolbars, drawers, info bar
const kBorderColor = Color(0xFF1A2A4A); // subtle dividers / borders
const kAccent = Color(0xFF2979FF); // Blue Accent 400 — active highlights
const kAccentActive = Color(0xFF0D1F4A); // dark bg for active tab / item
const kOrange = Color(0xFFFF6D00); // center marker, scanning indicator
const kTextPrimary = Colors.white;
const kTextSecondary = Color(0xFFB0BEC5); // Blue-grey 200
const kTextMuted = Color(0xFF546E7A); // Blue-grey 600
const kGreen = Color(0xFF4CAF50); // idle / ok status dot
