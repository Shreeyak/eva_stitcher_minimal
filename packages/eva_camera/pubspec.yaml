name: eva_camera
description: CameraX/Camera2 plugin with manual exposure, focus, WB, and diagnostics.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.2
  flutter: ">=3.29.0"

flutter:
  plugin:
    platforms:
      android:
        package: com.example.eva_camera
        pluginClass: EvaCameraPlugin

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
