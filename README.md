# eva_minimal_demo

An incremental stitching app for whole slide imaging.

## Docs

- [GPU→CPU Camera Frame Readback](docs/GPU_CPU_Readback.md) — how to use `FrameReadbackActivity` and the two readback methods (`TextureViewBitmapCapture` / `GpuToCpuSurfaceProvider`) for ML inference.
